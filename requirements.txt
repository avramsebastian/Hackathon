# ── Core simulation ───────────────────────────────────────────────────────
pygame>=2.6
numpy>=2.0
joblib>=1.4
scikit-learn>=1.5

# ── ML data generation & training (optional) ─────────────────────────────
pandas>=2.0

# ── REST API server (optional — only needed for ml/comunication/api.py) ──
fastapi>=0.100
uvicorn>=0.30
pydantic>=2.0

# ── Documentation (optional — only needed for `make html` in docs/) ──────
Sphinx>=7.0
sphinx-rtd-theme>=2.0
