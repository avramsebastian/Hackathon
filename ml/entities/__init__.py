"""
ml/entities — Domain data classes for feature extraction
========================================================

Classes
-------
Car
    Position, speed and direction of a vehicle.
Directions
    Enum of intended manoeuvres (LEFT, RIGHT, FORWARD).
Sign
    Enum of traffic sign types (STOP, YIELD, PRIORITY, NO_SIGN).
Intersections
    Builds the fixed-length feature vector consumed by the ML model.
"""
